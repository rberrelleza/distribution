name: registry
labels:
   app.kubernetes.io/component: registry
image: okteto/golang:1
command: bash
workdir: /usr/src/app/cmd/registry
securityContext:
  capabilities:
    add:
    - SYS_PTRACE
volumes:
- /go/pkg/
- /root/.cache/go-build/
sync:
- .:/usr/src/app
forward:
- 2345:2345
- 5000:5000
